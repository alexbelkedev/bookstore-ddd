spring:
  datasource:
    url: jdbc:h2:mem:bookstore;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.time_zone: UTC
  h2:
    console:
      enabled: true

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
ordering:
  payments:
    mode: http           # "local" to switch back
    base-url: http://localhost:8080

# activate logging for http client calls
#logging:
#  level:
#    org.springframework.web.client.RestClient: DEBUG
#    org.springframework.web.client: DEBUG
#    org.apache.hc.client5.http: DEBUG
#    org.apache.hc.client5.http.wire: DEBUG   # raw request/response lines
#    org.apache.hc.core5.http: DEBUG